import MainBackground from "./MainBackground";
import PropertyContainer from "./PropertyContainer";

const Body = () => {
  return (
    <div>
      <MainBackground />
      <PropertyContainer />
    </div>
  );
};

export default Body;
